# Input methods

The module installs IBus and Fcitx5 engines for Chinese input, including Rime and Mozc. Export captures IBus dconf settings plus the reviewed Fcitx5 `config` and `profile` files; it intentionally excludes user dictionaries and account data. Log out and back in after installation or import.
